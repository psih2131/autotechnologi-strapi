'use strict';

/**
 * partners-component router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::partners-component.partners-component');
