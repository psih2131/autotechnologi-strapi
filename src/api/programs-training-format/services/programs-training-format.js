'use strict';

/**
 * programs-training-format service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::programs-training-format.programs-training-format');
