'use strict';

/**
 * programs-training-format router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::programs-training-format.programs-training-format');
